if [ ! -f /etc/xinetd.d/tftp ]
then
    /usr/bin/mkdir -p /etc/xinetd.d
    /usr/bin/cp /etc/defaults/etc/xinetd.d/tftp /etc/xinetd.d/tftp
fi

if [ ! -f /etc/inetd.d/tftp ]
then
    /usr/bin/mkdir -p /etc/inetd.d
    /usr/bin/cp /etc/defaults/etc/inetd.d/tftp /etc/inetd.d/tftp
fi

#!/bin/bash

export TFTPD_CONFIG_AUTO_ANSWER_NO=1
/usr/bin/tftpd-config
